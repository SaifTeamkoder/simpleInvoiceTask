import React from 'react';
import {Box, Text} from 'native-base';

const CreateInvoice = ({navigation, route}) => {
  return <Box flex="1" bgColor="#FFF" p="5"></Box>;
};
export default CreateInvoice;
